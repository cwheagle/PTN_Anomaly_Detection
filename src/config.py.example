import os

# API 및 시스템 버전 (Semantic Versioning: Major.Minor.Patch)
API_VERSION_MAJOR = 1
API_VERSION_MINOR = 0
API_VERSION_PATCH = 0
API_VERSION = f"{API_VERSION_MAJOR}.{API_VERSION_MINOR}.{API_VERSION_PATCH}"

# Database Configuration
DB_CONFIG = {
    "host": os.getenv("DB_HOST", "YOUR_DB_HOST"),
    "user": os.getenv("DB_USER", "YOUR_DB_USER"),
    "password": os.getenv("DB_PASS", "YOUR_DB_PASSWORD"),
    "database": os.getenv("DB_NAME", "YOUR_DB_NAME"),
    "port": int(os.getenv("DB_PORT",YOUR_DB_PORT))
}

# --- Kafka & Redis Configuration (Phase 12) ---
KAFKA_CONFIG = {
    "bootstrap_servers": os.getenv("KAFKA_BROKERS", "YOUR_KAFKA_BROKERS"),
    "topic_metrics": os.getenv("KAFKA_TOPIC_METRICS", "ptn_metrics"),
    "consumer_group_id": os.getenv("KAFKA_CONSUMER_GROUP_ID", "ptn_inference_group"),
    "auto_offset_reset": "earliest"
}

REDIS_CONFIG = {
    "host": os.getenv("REDIS_HOST", "YOUR_REDIS_HOST"),
    "port": int(os.getenv("REDIS_PORT",YOUR_REDIS_PORT)),
    "db": 0,
    "decode_responses": True
}

# API Webhook Configuration
API_URL = os.getenv("API_URL", "YOUR_API_URL")
# ----------------------------------------------

# Feature Groups
FEATURE_GROUPS = {
    "traffic": ['tx_packet', 'rx_packet', 'error_packet'],
    "optical": ['tx_avg_power', 'rx_avg_power']
}

# Model Configuration (공통 아키텍처 및 분석 설정)
MODEL_CONFIG = {
    "hidden_dim": 32,
    "latent_dim": 16,
    "num_layers": 2,
    "window_size": 12,
    "batch_size": 32,
    "learning_rate": 0.001,
    "epochs": 100,
    "threshold_percentile": 99.5,   
    "patience": 10,
    "threshold": 0.1,        
    "slope_threshold": 0.5,  
    "rul_target": 90.0       # RUL 예측 목표 심각도 (Critical 임계치)
}

# Severity Configuration (고정 경보 등급 체계)
SEVERITY_CONFIG = {
    "NORMAL": {"min": 0, "max": 50, "level": 0, "label": "NORMAL"},
    "MINOR": {"min": 50, "max": 70, "level": 1, "label": "MINOR"},
    "MAJOR": {"min": 70, "max": 90, "level": 2, "label": "MAJOR"},
    "CRITICAL": {"min": 90, "max": 100, "level": 3, "label": "CRITICAL"}
}

# Signal Types
SIGNAL_TYPES = {
    "ETH": 6,
    "OPTIC": 44
}

# Path Configuration
PATHS = {
    "traffic": {
        "model": "models/traffic_ae.pth",
        "scaler": "models/traffic_scaler.joblib"
    },
    "optical": {
        "model": "models/optical_ae.pth",
        "scaler": "models/optical_scaler.joblib"
    },
    "log_path": "logs/anomaly_detection.log"
}

# Scheduler Configuration
INTERVAL_MINUTES = 15
RETENTION_DAYS = 30
